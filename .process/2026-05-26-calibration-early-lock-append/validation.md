# Validation: early-lock outcome-append assumptions

## Assumptions

1. The two target points exist in the canonical ledger at deterministic locations. Both `hypothesis_id` suffixes resolve to existing points. Verified: `data/calibration-ledger.json:blocks[0].points[0]` is `bunia -> kampala-uga` hid suffix `e77003de63` (Block-1, pinned 2026-05-20, resolves 2026-06-19); `data/calibration-ledger.json:blocks[1].points[2]` is `rwampara -> kampala-uga` hid suffix `92b62f4c0c` (Block-2, pinned 2026-05-21, resolves 2026-06-20). Confirmed by python json scan at canonical-ledger-path data/calibration-ledger.json:1.
2. The ledger today contains zero `outcome` fields anywhere. This append is the first ever. Verified: `grep -c '"outcome"' data/calibration-ledger.json` returns 0. Citation: data/calibration-ledger.json:1.
3. The resolver's `proposed_ledger_outcomes` block carries the exact append patch with the same five evidence keys for both target points: `classification`, `confirmed_in_window`, `first_in_window_confirmation_date`, `source_id`, `source_url`. Verified: report at `/tmp/lovs-canonical-data/resolution-report.json` shows both outcomes with `outcome=1`, `confirmed_in_window=true`, `first_in_window_confirmation_date=2026-05-23`, `source_id=uga-moh-2026-05-23-kampala-three-new`, matching for both corridors. Citation: calibration_resolver.py:239 (the proposed_ledger_outcomes builder).
4. The evidence feed entry for `target_zone: kampala-uga` is `resolution_eligible`, has `confirmed_in_window: true`, `first_in_window_confirmation_date: "2026-05-23"`, and `source_id: uga-moh-2026-05-23-kampala-three-new`. The same entry's `pre_window_cases` note explicitly excludes the 2026-05-15-16 Kampala imports (WHO DON602) from the resolution window, which is the right doctrine: those predate Block-1's `pinned_at` of 2026-05-20. Verified: data/calibration-resolution-evidence.json:1 contains the entry with all expected fields and the pre-window exclusion note.
5. The doctrine line authorizing this append already exists and is unambiguous about append-only semantics. Verified: `_meta.doctrine[3]` reads "Resolve by appending. On a resolution date, score the due points against public reports and append the outcome; the original pinned range is never altered." Citation: data/calibration-ledger.json:1.
6. The resolver's `_atomic_write_text` guard at `calibration_resolver.py:60-63` refuses writes to `LEDGER_PATH` by path identity. This guard remains in place; the founder-gated hand-edit through `Edit` is the correct channel for this operation and the only legal write path to the ledger. Verified by source read at calibration_resolver.py:57-66.
7. The ledger is not in the cross-surface publisher mirror set. The cross-surface parity gate hashes only the 4 paths in `WEBSITE_ASSETS` at `release_snapshot.py:100-105`; `data/calibration-ledger.json` does not appear there. Therefore the append does not require a paired website commit and does not affect cross-surface parity. Verified at release_snapshot.py:100.
8. The resolver derives outcomes purely from the evidence feed and never reads the ledger's `outcome` fields. Adding the field does not change the resolver's report contents; the new field is informational record-keeping only. Verified by source read of `resolve_point` at calibration_resolver.py:111-192: the function reads `risk_adj_50`, `pinned_at`, `resolves_at`, `target`, `hypothesis_id` from the ledger point and never consults `outcome`.
9. JSON formatting convention for ledger writes is indent=2, sort_keys=False (preserves source order). Verified by reading the existing file: each block's points are key-ordered as `hypothesis_id`, `corridor`, `source`, `target`, `horizon_days`, `risk_adj_50` (not alphabetical), confirming sort_keys=False is the convention. Citation: data/calibration-ledger.json:1.
10. The new monotonic-guard test can compare against the merge-base ledger via `git show origin/main:data/calibration-ledger.json` rather than `HEAD~1` (which is undefined when the branch has multiple commits or is rebased). Verified: `git show origin/main:data/calibration-ledger.json` returns the 2-block, 12-point ledger as expected at the branch base commit `5aebd16`. Citation: external git command, verified at release_snapshot.py:46 REPO_ROOT.
