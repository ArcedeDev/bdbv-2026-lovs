# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
*.log

# Virtual environments
.venv/
venv/
env/
ENV/
.env
.env.*
!.env.example

# Editors / OS
.vscode/
.idea/
.DS_Store
*.swp
*.swo

# Optional: pip-installed convertors (chromium etc.) profile dirs
.chrome-profile/
# Brief artifacts (brief/brief.html, brief/visuals/*, deliverables/brief.pdf)
# are committed so they are accessible from GitHub without cloning. Regenerate
# them locally via `python make_brief.py`.

# Internal Arcede engineering-pipeline scaffolding (plan.md, validation.md,
# review.md, etc.). Not for public consumption.
.process/

# Stray legacy deliverable artifacts not tracked in this repo.
deliverables/Ebola_2026_brief.pdf

# Restricted publisher material retained locally for audit but not
# redistributed in the public repository.
data/bundibugyo-2026/private/
data/bundibugyo-2026/poe_traveler_counts*.restricted.json
data/external_sources/cache/
data/external_sources/*-brief.md
# Generated by scheduled source-prep jobs; curated non-slot freshness anchors
# may still be tracked deliberately.
data/external_sources/freshness/*-africa_*.json
data/external_sources/freshness/*-americas_*.json
data/external_sources/freshness/*-weekly_*.json
data/external_sources/prep/
data/external_sources/health/
DIRTY_TREE_TRIAGE_*.md
*.restricted.json
*.restricted.csv
*.restricted.xlsx
*.restricted.pdf

# Generated read-only calibration resolution report (regenerable via calibration_resolver.py --write-report)
data/calibration-resolution-report.json

# Generated read-only cycle-status + routing-plan (regenerable via cycle_status.py).
# Internal prep/routing artifact; not part of the public release surface.
deliverables/cycle-status/
